New Group Form

<%= form_for @group do |form| %>

 <%= form.label :name %>
 <%= form.text_field :name %>
  <%= tag(:br) %>
  <%= tag(:br) %>
   <%= form.label :icon %>
 <%= form.text_field :icon %> (hr)
 <%= form.submit  %>
<% end %>